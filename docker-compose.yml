services:
  backend:
    build:
      context: ./backend
    ports:
      - "3001:8080"
    environment:
      - PORT=8080

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
